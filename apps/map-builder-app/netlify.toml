# netlify.toml cho map-builder-app
# Cấu hình Netlify UI:
# - Base directory: (để trống)
# - Package directory: apps/map-builder-app
# - Build command: cd ../.. && pnpm turbo run build --filter @repo/map-builder-app
# - Publish directory: dist

[build]
  # Build command chạy từ root của monorepo (cd về root từ package directory)
  command = "cd ../.. && pnpm turbo run build --filter @repo/map-builder-app"
  # Publish directory relative to package directory (apps/map-builder-app/dist)
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

